#include <stdio.h>

/**
 * main - fibonacci <3
 *
 * Purpose - no hardcode
 *
 * Return:  (Success)
 */
int main(void)
{
	char ch;

	for (ch = 0; ch < 10; ch ++)
	{
		putchar(ch);
		ch++;
		return (0);
	}
}

